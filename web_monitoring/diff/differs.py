from web_monitoring_diff.basic_diffs import (
    compare_length,
    identical_bytes,
    side_by_side_text,
    compute_dmp_diff,
    html_text_diff,
    html_source_diff,
)
# Suppress Pyflakes Warnings
assert compare_length
assert identical_bytes
assert side_by_side_text
assert compute_dmp_diff
assert html_text_diff
assert html_source_diff
